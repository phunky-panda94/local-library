extend layout 

block content 
    h1= title 

    each genre in genres 
        h5
            -var url=`/catalog/genres/${genre}/books`
            a(href=url) #{genre}
    else 
        h5 No genres in the current catalog