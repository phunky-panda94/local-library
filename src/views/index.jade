extends layout

block content
  h1 Welcome to the Local Library

  h3 Catalog

  if error
    p Error getting dynamic content.
  else
    p The library has the following catalog:

    ul
      li #[strong Books:] !{data.bookCount}
      li #[strong Authors:] !{data.authorCount}
